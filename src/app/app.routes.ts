import { Route } from '@angular/router';
import { MarkdownInputComponent } from './markdown-input/markdown-input.component';

export const appRoutes: Route[] = [ { path: '', component: MarkdownInputComponent }];
